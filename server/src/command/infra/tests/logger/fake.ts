import Logger from '../../../@core/commom/application/logger/logger';

export default class LoggerFake implements Logger {
    error(...message: unknown[]): void {
        
    }

    info(...message: unknown[]): void {
        
    }

    warning(...message: unknown[]): void {
        
    }
}
